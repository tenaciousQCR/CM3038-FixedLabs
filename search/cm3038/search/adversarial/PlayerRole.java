package cm3038.search.adversarial;

/**
 * This enumeration type models the 2 player roles in a Minimax game.
 * The UNKNOWN value is just there for safety reason (in case we need it).
 * @author kit
 *
 */
public enum PlayerRole {
    MAX, MIN, UNKNOWN;
}
